fn main() {
    let x: i32 = 42;       // Entero
    let y: f64 = 3.14;     // Flotante
    let is_active: bool = true;  // Booleano
    let nombre: &str = "Rust";   // Cadena de texto (String literal)

    println!("x: {}, y: {}, is_active: {}, nombre: {}", x, y, is_active, nombre);
}
